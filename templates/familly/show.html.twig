{% extends 'base.html.twig' %}

{% block title %}Familly{% endblock %}

{% block body %}
    <h1>Familly</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ familly.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ familly.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('familly_index') }}">back to list</a>

    <a href="{{ path('familly_edit', {'id': familly.id}) }}">edit</a>

    {{ include('familly/_delete_form.html.twig') }}
{% endblock %}
