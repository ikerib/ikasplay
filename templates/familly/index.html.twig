{% extends 'base.html.twig' %}

{% block title %}Familly index{% endblock %}

{% block bodya %}
    <h1>Familiak</h1>

    <a class="btn btn-primary" href="{{ path('familly_new') }}">+ Berria</a>

    <hr>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for familly in famillies %}
            <tr>
                <td>{{ familly.id }}</td>
                <td>{{ familly.name }}</td>
                <td>
                    <a href="{{ path('familly_show', {'id': familly.id}) }}">show</a>
                    <a href="{{ path('familly_edit', {'id': familly.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('familly_new') }}">Create new</a>
{% endblock %}
